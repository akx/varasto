{
  "name": "varasto-core",
  "private": true,
  "engines": {
    "node": ">=8.0"
  },
  "scripts": {
    "build": "lerna run build",
    "watch": "lerna run watch"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
